# Startup ordering: MetalLB must start first for LoadBalancer IPs
controller:
  priorityClassName: system-cluster-critical

speaker:
  priorityClassName: system-cluster-critical
  securityContext:
    privileged: true
    capabilities:
      add:
        - NET_ADMIN
        - NET_RAW
