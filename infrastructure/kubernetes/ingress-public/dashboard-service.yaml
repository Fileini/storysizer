apiVersion: v1
kind: Service
metadata:
  name: traefik-public-dashboard
  namespace: ingress
  labels:
    app: traefik-public
spec:
  type: ClusterIP
  selector:
    app: traefik-public
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080